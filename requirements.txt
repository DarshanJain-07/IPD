# OpenCV for video capture and image processing
opencv-python>=4.5.5.64

# NumPy for numerical operations
numpy>=1.19.5

# MediaPipe for face landmark detection (ensure you are using a supported Python version)
mediapipe>=0.10.21

# PyWavelets for wavelet denoising
PyWavelets>=1.3.0

# Matplotlib for plotting results
matplotlib>=3.3.4

# SciPy for signal processing functions (detrend, filtering, FFT, etc.)
scipy>=1.5.4
